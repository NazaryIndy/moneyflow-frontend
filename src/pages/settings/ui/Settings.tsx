import type { FC } from 'react';
import { PageContainer, PageTitle } from '@/shared/ui';

const Settings: FC = () => {
  return (
    <PageContainer>
      <PageTitle title="Settings" />
    </PageContainer>
  );
};

export { Settings };
