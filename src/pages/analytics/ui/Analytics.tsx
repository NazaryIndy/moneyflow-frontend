import type { FC } from 'react';
import { PageContainer, PageTitle } from '@/shared/ui';

const Analytics: FC = () => {
  return (
    <PageContainer>
      <PageTitle title="Analytics" />
    </PageContainer>
  );
};

export { Analytics };
